<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
    <title>TinyApp</title>
  </head>
  <body style="font-size:20px; padding:50px;">
    <% include partials/_header %>
    <h2>My URLs</h2>
    <ul>
      <% for (let url in urls) { %>
        <form method="POST" action="/urls/<%= url %>?_method=DELETE">
          <li>
            <%= url %>:
            <a href="/u/<%= url %>"><%= urls[url].longURL %></a>
            <br>
            <a href="/urls/<%= url %>">Edit</a>
            <button class="btn btn-primary">Delete</button>
            <br>
            Created: <%= urls[url].created %>
            <br>
            Total Visits: <%= urls[url].visits %>
            <br>
            Unique Visits: <%= Object.keys(urls[url].visitors).length + (urls[url].visitors["Anonymous"].length || 0) - 1 %>
          </li>
        </form>
      <% }%>
    </ul>
  </body>
</html>
